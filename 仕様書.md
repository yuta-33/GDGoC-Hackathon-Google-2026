# Pet Fashion Try-On / PetFit AI 仕様書

## 1. 文書の目的

本書は、`flutter_app` と `backend` の現状実装をリバースエンジニアリングし、2026-03 時点の実装仕様を整理したものである。

対象範囲:

- Flutter フロントエンド
- 将来追加予定の Web フロントエンド
- Python Flask バックエンド
- フロントエンドとバックエンドの現在の接続仕様
- ローカル保存仕様
- 既知の制約、未実装項目

本書は「理想仕様」ではなく「現在の実装仕様」をまとめる。ただし、企画ドラフトとの乖離が誤解を生まないように、企画上の目標像と現状実装スコープの差分も明記する。

前提条件:

- 本プロダクトは Google 主催ハッカソン向けの提出を想定しており、インフラは Google Cloud Platform を前提に構成する
- backend, DB, 画像保存, 機密情報管理は可能な限り GCP サービスへ寄せる方針とする

## 2. システム概要

本システムは、企画上は「Pet Fashion Try-On」という名称で整理されている。現状のアプリ実装名は `PetFit AI` であり、両者は同一プロダクトの企画名と実装名の関係にある。

企画コンセプト:

- ペット版デジタルクローゼット
- AI バーチャル試着
- コーデ提案
- 比較、保存、共有

現状実装の位置づけ:

- 上記コンセプトのうち、MVP に相当する一部だけを先行実装している
- 現在は「ペットプロフィール入力」「商品閲覧」「簡易 try-on 画面」「breed baseline 取得」「AI ダミー接続」までが中心
- クローゼット管理、保存コーデ、共有、実画像ベースの try-on は未実装

したがって、企画との方向性は一致しているが、実装スコープはまだ限定的である。

本システムは、ペット向けファッションアプリのモバイルフロントエンドと、AI 関連ダミー応答を返す最小バックエンドで構成される。

構成:

- `flutter_app`
  - Flutter 3.x
  - Riverpod による状態管理
  - 画面遷移、ペットプロフィール入力、AI try-on、手持ち服選択 UI
- `web_app`（追加予定）
  - Next.js
  - Flutter 版と同等の MVP 画面を Web 公開用に実装予定
  - Vercel へのデプロイを前提とする
- `backend`
  - Python 3.11
  - Flask
  - `pets / closet / saved-looks` は store abstraction で永続化
  - ローカルでは SQLite、GCP / Cloud Run では Firestore を利用する構成へ移行中
  - Google Cloud Run へ `--source .` でデプロイ可能
  - breed baseline API と AI ダミー API を提供

GCP 前提方針:

- 現状の backend 実行基盤は Cloud Run
- 今後の本番寄り構成は GCP を前提とし、DB は Firestore を第一候補、画像は Cloud Storage、シークレットは Secret Manager を候補とする
- 現在の SQLite はローカル開発 fallback であり、提出構成では managed GCP サービスを前提とする

接続形態:

- Flutter アプリは Cloud Run 上のバックエンドへ直接 HTTPS アクセスする
- 将来の Web フロントエンドも同一 Cloud Run backend を利用する想定である
- Web 版を追加する場合は Cloud Run API に対して CORS 対応が必要になる
- Web 版で扱う pet photo / outfit image は Cloud Storage 保存へ移行する前提とする
- 現在の API ベース URL は [api_config.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/core/config/api_config.dart) に固定値で定義されている
- URL: `https://petfashion-backend-604416065934.asia-northeast1.run.app`

## 3. リポジトリ構成

主要ディレクトリ:

- `flutter_app/lib/app`
  - アプリ本体、ルーター、テーマ
- `flutter_app/lib/core`
  - 共通モデル、共通ウィジェット、共通設定
- `flutter_app/lib/features`
  - 機能別 UI、状態、モックデータ、API 接続
- `backend`
  - Flask API、Cloud Run デプロイ設定、breed baseline データ

## 4. フロントエンド仕様

### 4.1 技術構成

- フレームワーク: Flutter
- 状態管理: Riverpod
- 画像選択: `image_picker`
- ファイル選択: `file_selector`
- ローカル保存先取得: `path_provider`
- HTTP 通信: `dart:io` の `HttpClient`

補足:

- launch 後またはハッカソン展示向けの配布経路として、同等機能の Web フロントエンドを Next.js で追加する方針を採る
- Web 版のホスティング先は Vercel を第一候補とする

### 4.2 画面遷移

ルーティングは [app_router.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/app/router/app_router.dart) で集中管理されている。

画面一覧:

- `/`
  - Splash
- `/onboarding`
  - Onboarding
- `/pet-profile`
  - Pet Profile
- `/home`
  - Home
- `/shop`
  - Shop
- `/try-on`
  - Try-On
- `/pets`
  - Pets
- `/profile`
  - Profile
- `/closet`
  - Closet
- `/saved-looks`
  - Saved Looks
- `/search`
  - Search
- `/product`
  - Product Detail
- `/brand`
  - Brand
- `/size-guide`
  - Size Guide
- `/notifications`
  - Notifications

主導線:

1. Splash
2. Onboarding
3. Pet Profile
4. Home
5. Bottom Navigation で Shop / Try-On / Pets / Profile へ遷移

### 4.3 フロントエンド機能一覧

#### Splash / Onboarding

- Splash 表示後、自動で Onboarding へ遷移
- Onboarding は 3 ページ構成
- `Next` / `Skip` / `Get Started` で Pet Profile へ進む

#### Pet Profile

実装ファイル:

- [pet_profile_page.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/features/pet_profile/presentation/pages/pet_profile_page.dart)

入力項目:

- Pet Name
- Breed
- Weight
- Unit 切替 `KG / LB`
- Neck Girth
- Chest Girth
- Back Length
- Pet Photo

企画ドラフトとの差分:

- 現在の方針は `犬中心` であり、犬プロフィールの深さを優先している
- 年齢、性別、毛色、サイズ感メモなどの追加項目はまだ未実装

現在の挙動:

- Breed 候補は backend の `/breeds` から取得する
- Breed を選択すると、その breed の標準 measurements がフォームに自動入力される
- 自動入力後もユーザーが自由に数値を書き換えられる
- `Save Profile` 時に:
  - フロント側の `petProfileProvider` に保存
  - backend の `/analyze-pet` を呼び出し
  - 結果を `SnackBar` と `debugPrint` に出力
  - その後 Home へ遷移

#### Home

- フィーチャー商品表示
- ブランド一覧表示
- 商品グリッド表示
- DemoState の切替
- 通知画面や検索画面への遷移

#### Shop

- Bottom Navigation 上には残している
- ただし launch build では `Coming Soon` 画面を表示する

企画ドラフトとの差分:

- 企画上の「Closet / Wardrobe」は未実装
- launch 時点では commerce 体験を閉じ、Pet Profile と Try-On を主導線とする

#### Search

- launch build では `Coming Soon`

#### Product Detail

- launch build では `Coming Soon`

#### Try-On

- 左右スワイプによる服プリセット選択
- `Try This Outfit` ボタン押下時のみ Vertex AI ベースの生成リクエストを実行
- 生成中 overlay 表示
- 生成済みプレビューの端末内キャッシュ
- 服サムネイル、購入元、サイズ、素材、厚み等のメタ情報表示

注意:

- 服カードはローカル asset または公開画像 URL から表示する
- 生成結果は backend `/tryon-demo` を経由して受け取る
- 同じ pet / 同じ outfit の再表示はローカルキャッシュで高速化する
- 完全な比較 UI や複数候補の同時表示は未実装

企画ドラフトとの差分:

- 企画上は「比較」「保存」「共有」まで含む
- 現状は 1 枚の pet photo に対し 1 outfit ずつ生成する MVP である
- 服比較や複数生成結果の並列表示はまだ未実装

#### Pets

- 保存済み pet profile の表示
- 写真がある場合はローカル保存した写真をアバター表示
- `Virtual Try-On` / `Size Guide` / `Edit` 導線

#### Profile

- スタブ画面
- 各種設定項目は bottom sheet で `coming soon` 表示が中心
- `My Closet` 導線から Closet 画面へ遷移できる

#### Closet

- backend `/closet/items` 一覧を表示
- `Add Item` から手持ち服を追加可能
- `Save Look` から 1 アイテム構成の saved look を保存可能
- 現時点では編集 / 削除は未実装

#### Saved Looks

- backend `/saved-looks` 一覧を表示
- 保存済み look の thumbnail と item 名を表示
- 現時点では再編集や削除は未実装

#### Notifications

- モック通知一覧
- DemoState に応じて empty / error を表示可能

未実装の企画機能:

- デジタルクローゼット管理
- 服情報解析
- 保存済みコーデ一覧
- 共有機能
- 複数ペット比較
- 購入履歴連携
- 公開 Web フロントエンド

### 4.4 状態管理

主な状態:

- `petProfileProvider`
  - 現在のペットプロフィール
- `demoStateProvider`
  - `normal / loading / empty / error`
- `productsProvider`
  - 商品一覧
- `brandsProvider`
  - ブランド一覧
- `notificationsProvider`
  - 通知一覧
- `breedBaselinesProvider`
  - backend の `/breeds` から取得した breed baseline 一覧

特徴:

- 商品、ブランド、通知は Riverpod の `FutureProvider`
- ペットプロフィールは Riverpod の `StateNotifierProvider`
- breed baseline も Riverpod の `FutureProvider`

### 4.5 データモデル

#### PetProfile

定義: [pet_profile.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/core/models/pet_profile.dart)

項目:

- `id`
- `name`
- `breed`
- `weight`
- `neckGirth`
- `chestGirth`
- `backLength`
- `photoPath`

備考:

- `photoPath` はアプリ内ローカルファイルの絶対パス
- Flutter 版では backend へそのままアップロードしていない
- Web 版追加時は `photoPath` ではなく Cloud Storage URL を扱う前提へ移行する

#### BreedBaseline

定義: [breed_baseline.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/core/models/breed_baseline.dart)

項目:

- `id`
- `displayName`
- `species`
- `weightKg`
- `neckGirthCm`
- `chestGirthCm`
- `backLengthCm`

仕様:

- breed ごとの measurements は「標準値」で保持
- 範囲では保持しない
- フォームの初期入力補助として使い、最終値はユーザー入力を優先する

#### Product

定義: [product.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/core/models/product.dart)

項目:

- `id`
- `name`
- `brand`
- `price`
- `image`
- `category`
- `tags`
- `description`
- `sizes`
- `colors`
- `matchPercentage`

### 4.6 写真保存仕様

実装ファイル:

- [pet_photo_storage_service.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/features/pet_profile/application/pet_photo_storage_service.dart)

仕様:

- 画像選択は `image_picker` を利用
- カメラ撮影、ギャラリー選択、ファイル選択に対応
- 画像はアプリの documents directory 配下の `pet_photos/` にコピー保存
- ファイル名は `pet_<timestamp>.<ext>`
- 以前の写真がある場合は新規保存時に古いローカルファイルを削除
- 画像の実体はローカルに保存される
- `PetProfile` 本体は `shared_preferences` に保存されるため、アプリ再起動後も `photoPath` を再参照できる
- simulator 検証用に bundled test photo を読み込む導線を持つ

保存場所の論理構造:

- `ApplicationDocumentsDirectory/pet_photos/`

### 4.7 フロントから backend への接続仕様

#### /breeds

利用箇所:

- Pet Profile の breed 候補

実装ファイル:

- [breed_baseline_service.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/features/pet_profile/application/breed_baseline_service.dart)

用途:

- breed baseline 一覧の取得
- breed 選択時の標準 measurements 反映

#### /analyze-pet

利用箇所:

- Pet Profile 保存時

実装ファイル:

- [pet_analysis_service.dart](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/lib/features/pet_profile/application/pet_analysis_service.dart)

送信内容:

- `imageUrl`
  - 現在はダミー URL 固定
- `petName`
- `breed`
- `weight`
- `neckGirth`
- `chestGirth`
- `backLength`

現在の制約:

- ローカルに保存した pet photo は backend に送っていない
- 画像アップロード API は未実装
- Flutter 版はローカル写真を base64 化して `/tryon-demo` へ送る
- 永続化された画像 URL 管理は未実装

## 5. バックエンド仕様

### 5.1 技術構成

- 言語: Python
- フレームワーク: Flask
- デプロイ先: Google Cloud Run
- 起動ポート: `PORT` 環境変数
- コンテナ起動: `gunicorn`
- データストア:
  - local: SQLite
  - Cloud Run / GCP: Firestore

主ファイル:

- [app.py](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/backend/app.py)
- [db.py](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/backend/db.py)
- [breed_baselines.py](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/backend/breed_baselines.py)
- [Dockerfile](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/backend/Dockerfile)

補足:

- 可変データは store abstraction で管理される
- ローカル実行時は SQLite ファイル `backend/petfashion.db`
- Cloud Run 実行時は Firestore を既定で使用
- breed baseline は引き続き Python の静的マスタ
- Firestore 利用時は Cloud Run の Application Default Credentials で接続する想定

### 5.2 API 一覧

#### GET /health

用途:

- ヘルスチェック

レスポンス:

```json
{
  "ok": true,
  "service": "petfashion-backend"
}
```

#### GET /breeds

用途:

- breed baseline 一覧取得

レスポンス形式:

```json
{
  "ok": true,
  "items": [
    {
      "id": "jack_russell_terrier",
      "displayName": "Jack Russell Terrier",
      "species": "dog",
      "weightKg": 7.0,
      "neckGirthCm": 26.0,
      "chestGirthCm": 40.0,
      "backLengthCm": 30.0
    }
  ]
}
```

#### GET /breeds/{breedId}

用途:

- breed baseline 単体取得

正常レスポンス:

```json
{
  "ok": true,
  "item": {
    "id": "golden_retriever",
    "displayName": "Golden Retriever",
    "species": "dog",
    "weightKg": 28.5,
    "neckGirthCm": 42.0,
    "chestGirthCm": 68.0,
    "backLengthCm": 55.0
  }
}
```

異常レスポンス:

```json
{
  "ok": false,
  "error": "Breed not found"
}
```

#### POST /analyze-pet

用途:

- ペット解析ダミー応答
- 入力された breed が baseline に存在するか判定し、`matchedBreedId` を返す

現在の入力例:

```json
{
  "imageUrl": "https://example.com/dummy-pet-image.png",
  "petName": "Buddy",
  "breed": "Golden Retriever",
  "weight": 28.5,
  "neckGirth": 42.0,
  "chestGirth": 68.0,
  "backLength": 55.0
}
```

現在のレスポンス例:

```json
{
  "ok": true,
  "result": {
    "petType": "dog",
    "furColor": "white",
    "bodySize": "medium",
    "styleTags": ["cute", "casual"],
    "matchedBreedId": "golden_retriever"
  }
}
```

#### PUT /pets/{petId}

用途:

- 既存ペットプロフィール更新
- Flutter の Pet Profile 編集保存時に同一 pet を上書きする

#### POST /recommend-outfits

用途:

- コーデ提案ダミー応答

現在のレスポンス例:

```json
{
  "ok": true,
  "items": [
    {
      "id": "outfit_01",
      "name": "Pastel Hoodie",
      "color": "blue",
      "matchReason": "Matches light fur tones",
      "imageUrl": "https://example.com/outfit_01.png"
    }
  ]
}
```

#### POST /tryon-demo

用途:

- try-on 結果生成
- Vertex AI ベースの試着画像生成を返す経路

現在のレスポンス例:

```json
{
  "ok": true,
  "previewMode": "vertex",
  "generatedImageBase64": "<base64>",
  "generatedImageMimeType": "image/png",
  "resultImageUrl": "https://example.com/tryon_result.png"
}
```

補足:

- 画像未送信時や生成失敗時は overlay fallback を返す場合がある
- Flutter 側では生成結果を端末内 cache に保存して再利用する

### 5.3 breed baseline マスタ仕様

定義ファイル:

- [breed_baselines.py](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/backend/breed_baselines.py)

現状:

- 犬種ごとに標準 measurements を 1 セット保持
- 代表的な犬種に加え、`Jack Russell Terrier` を含む
- species は現時点で `dog` 固定

保持方針:

- `weightKg`
- `neckGirthCm`
- `chestGirthCm`
- `backLengthCm`

注意:

- 範囲ではなく標準値を保持
- 最終 measurements はユーザー入力値を優先する想定

## 6. データ永続化仕様

### 6.1 現在永続化していないもの

- 認証情報
- 通知
- 商品情報
- AI 解析結果
- カート

### 6.2 現在ローカル保存しているもの

- pet photo の実ファイル
- `shared_preferences` 上の pet profile JSON
- try-on 生成結果のキャッシュ JSON
- backend の SQLite または Firestore に保存される pets / closet / saved looks

注意:

- pet photo ファイルはローカルに残る
- `photoPath` は pet profile JSON に含まれるため、再起動後も再参照できる
- try-on 生成結果は端末内 cache に保持され、同一 outfit 再表示の高速化に使う
- SQLite fallback はローカル専用であり、提出用の Cloud Run 構成では Firestore を前提とする

## 7. 非機能仕様

### 7.1 backend デプロイ

- Cloud Run source deploy 対応
- `gcloud run deploy ... --source .` でデプロイ可能

### 7.2 iOS 権限

- カメラ利用文言あり
- フォトライブラリ利用文言あり

設定ファイル:

- [Info.plist](/Users/sasayuta/Documents/GDGoC-Hackathon-Google-2026/flutter_app/ios/Runner/Info.plist)

### 7.3 将来の Web 配布方針

- 別フォルダで `web_app` を追加し、Next.js で Flutter 版と同等の MVP を実装する想定
- デプロイ先は Vercel を第一候補とする
- backend は Cloud Run を共通利用する
- Web ブラウザから Cloud Run を直接呼ぶため、CORS 対応が必要
- Web 版の pet photo / outfit image は Cloud Storage 保存を前提とする

## 8. 既知の制約・未実装事項

### 8.1 AI ロジック

- `/analyze-pet` はダミー応答
- `/recommend-outfits` はダミー応答
- `/tryon-demo` は Vertex AI ベースの生成経路を持つが、速度と安定性は改善余地がある
- 実画像解析、実推薦は未実装
- 実 try-on は MVP レベルであり、比較 UI や高品質な商品条件制御は未実装

### 8.2 ストレージ / DB / 認証

- Firestore 導入済み
- Cloud Storage 未導入
- Secret Manager 未導入
- 認証未導入
- Web フロント追加時は CORS と Cloud Storage を優先実装対象とする

補足:

- ハッカソン提出前提では GCP 利用が設計方針であり、現時点で未導入の部分は今後 GCP サービスへ寄せていく

### 8.3 フロントの既知課題

- pet photo はローカル保存のみで backend 送信されない
- Closet は接続済みだが、編集 / 削除は未実装
- Saved Looks は接続済みだが、再編集や削除は未実装
- `/pets` は接続済みだが、複数 pet 管理 UI と編集導線はまだ簡易実装
- launch build では `Shop / Search / Product Detail / Brand` は `Coming Soon`
- Amazon など外部ソースの outfit metadata は追加済みだが、登録件数はまだ少ない
- Web フロントは未着手

### 8.4 Android 側注意点

- `Image.network` を多用しているため、release 向け AndroidManifest 側の通信権限整備は別途確認が必要

## 9. 企画ドラフトとの整合チェック

### 9.1 一致している点

- 方向性は「ペット向けファッション支援アプリ」で一致している
- ペットプロフィール起点で体験を始める構成は一致している
- AI を使った分析、提案、try-on を中核体験に置く考え方は一致している
- 将来的に比較、保存、商品導線へつなぐ思想も一致している

### 9.2 現状の乖離点

- 企画名は `Pet Fashion Try-On`、実装名は `PetFit AI`
- 現在の正式方針は `dog` 中心であり、猫対応はスコープ外
- 企画の中核である `デジタルクローゼット` は未実装
- 企画の `保存 / 共有 / look 管理` は未実装
- try-on は MVP として実画像生成を含むが、UX と速度は未成熟
- AI は一部 Vertex AI 経路へ移行済みだが、推薦や解析はまだダミー応答を含む
- プロフィール項目は企画より少ない

### 9.3 本仕様書での修正方針

- 企画コンセプトは `Pet Fashion Try-On`
- 現状実装は `PetFit AI` という MVP
- 仕様書内では「企画の目標像」と「現在実装されている範囲」を分けて記述する

この整理により、企画との方向性を保ちつつ、現在のリポジトリ状態も正確に表現する。

## 10. 現在の動作確認済み項目

- backend ローカル起動
- backend Cloud Run デプロイ
- `/health` 疎通
- `/breeds` 疎通
- `/analyze-pet` 疎通
- backend SQLite 永続化
- backend Firestore 対応コード追加
- Flutter から Cloud Run `/analyze-pet` 呼び出し
- Flutter で breed baseline 自動入力
- Flutter で pet photo 選択とローカル保存
- Flutter で pet photo の file import と bundled test photo 読み込み
- Flutter で pet profile ローカル永続化
- Flutter から backend `/pets` 保存と一覧取得
- Flutter から backend `/closet/items` 一覧取得と追加
- Flutter から backend `/saved-looks` 一覧取得と保存
- Flutter Try-On の Vertex AI 経路
- Try-On 生成結果のローカル cache 再利用
- outfit preset への商品画像・サイズ・購入元メタ情報表示

## 11. 次の実装候補

優先候補:

1. pet photo の Cloud Storage 保存と URL 管理
2. Web フロントエンド `web_app` の新設と Vercel デプロイ
3. Cloud Run API の CORS 対応
4. outfit DB 拡張と追加商品登録フロー
5. `/recommend-outfits` の実ロジック化
6. Try-On の比較 / 保存 / 共有導線
